# Agent 3Bd V2 - هيكل المعلومات

## هيكل التنقل الرئيسي

### التنقل السفلي (Bottom Navigation)
التطبيق يحتوي على 4 تبويبات رئيسية:

1. **الرئيسية** 🏠 - Home Screen
2. **المحادثة** 💬 - Chat Screen
3. **الأدوات** 🛠️ - Tools Screen
4. **الإعدادات** ⚙️ - Settings Screen

## تفاصيل كل شاشة

### 1. الشاشة الرئيسية (Home Screen)

#### الأقسام:
- **شريط البحث السريع**
  - حقل بحث علوي
  - تصفية حسب النوع
  - ترتيب حسب التاريخ/الأهمية

- **قائمة الوكلاء النشطين**
  - أيقونة الوكيل
  - اسم الوكيل
  - حالة الاتصال (متصل/غير متصل)
  - عدد المحادثات النشطة

- **المحادثات الأخيرة**
  - معاينة الرسائل (آخر 3 رسائل)
  - اسم الوكيل
  - وقت آخر تفاعل
  - حالة المحادثة (نشطة/منتهية)

- **إجراءات سريعة**
  - زر "محادثة جديدة"
  - زر "إضافة وكيل جديد"
  - زر "تبديل المزود"

### 2. شاشة المحادثة (Chat Screen)

#### الأقسام:
- **رأس الشاشة**
  - اسم الوكيل النشط
  - أيقونة المزود (DeepSeek, Groq, OpenAI, Together)
  - حالة الاتصال
  - قائمة منسدلة لتغيير الوكيل

- **سجل المحادثة**
  - فقاعات محادثة للمستخدم
  - فقاعات محادثة للوكيل
  - عرض الصور/الملفات المرفقة
  - حالة الرسالة (مرسلة/جاري الإرسال/فشل)

- **حقل الإدخال**
  - حقل نص لكتابة الرسائل
  - أيقونة إرسال
  - أيقونة إرفاق ملف/صورة
  - أيقونة تسجيل صوتي

- **شريط الأدوات السفلي**
  - أدوات متاحة (Agent Tool, Edit Tool, Memory Tool, إلخ)
  - حالة الوكيل (يكتب، جاهز، خطأ)

### 3. شاشة الإعدادات (Settings Screen)

#### الأقسام:
- **المفاتيح API**
  - DeepSeek API Key
  - Groq API Key
  - OpenAI API Key
  - Together API Key
  - زر اختبار الاتصال لكل مزود

- **النماذج**
  - اختيار النموذج الافتراضي
  - إعدادات درجة الحرارة (Temperature)
  - إعدادات الحد الأقصى للرموز (Max Tokens)

- **الأذونات**
  - قائمة الأذونات المطلوبة
  - تبديل تفعيل/تعطيل لكل إذن
  - زر "طلب جميع الأذونات"

- **المظهر**
  - اختيار المظهر (فاتح/داكن)
  - حجم الخط
  - اللغة

### 4. إدارة الوكلاء (Agent Management)

#### الأقسام:
- **قائمة الوكلاء**
  - شبكة عرض الوكلاء
  - أيقونة كل وكيل
  - اسم الوكيل
  - وصف الوكيل
  - حالة الوكيل (نشط/غير نشط)

- **تفاصيل الوكيل**
  - معلومات الوكيل الكاملة
  - السياق والتعليمات الخاصة
  - الإعدادات المخصصة

- **إجراءات الوكيل**
  - إنشاء وكيل جديد
  - تعديل وكيل موجود
  - حذف وكيل
  - تكرار وكيل

### 5. شاشة الذاكرة (Memory Screen)

#### الأقسام:
- **سجل البيانات**
  - قائمة البيانات المحفوظة
  - نوع كل بيان (نص، صورة، ملف)
  - حجم البيانات
  - تاريخ الحفظ

- **السياق المحفوظ**
  - المحادثات المؤرشفة
  - تفضيلات المستخدم
  - بيانات السياق

- **إدارة الذاكرة**
  - حذف عناصر محددة
  - مسح جميع البيانات
  - تصدير البيانات
  - البحث في الذاكرة

### 6. شاشة الأدوات (Tools Screen)

#### الأقسام:
- **قائمة الأدوات المتاحة**
  - Agent Tool: إطلاق وكيل جديد
  - Edit Tool: تحرير الملفات والكود
  - Memory Tool: إدارة الذاكرة
  - Batch Execution: تنفيذ مهام متعددة
  - Search Tool: البحث في الملفات
  - Clear Tool: تنظيف البيانات

- **حالة الأدوات**
  - متاحة/غير متاحة
  - قيد الاستخدام
  - مطلوب إذن

- **تكوين الأدوات**
  - إعدادات كل أداة
  - تخصيص سلوك الأداة
  - اختصارات لوحة المفاتيح

## هيكل البيانات

### نموذج الوكيل (Agent Model)
```typescript
interface Agent {
  id: string;
  name: string;
  description: string;
  icon: string;
  systemPrompt: string;
  provider: 'deepseek' | 'groq' | 'openai' | 'together';
  model: string;
  isActive: boolean;
  createdAt: Date;
  updatedAt: Date;
}
```

### نموذج المحادثة (Chat Model)
```typescript
interface Chat {
  id: string;
  agentId: string;
  messages: Message[];
  status: 'active' | 'completed' | 'error';
  createdAt: Date;
  updatedAt: Date;
}
```

### نموذج الرسالة (Message Model)
```typescript
interface Message {
  id: string;
  chatId: string;
  role: 'user' | 'assistant' | 'system';
  content: string;
  attachments?: Attachment[];
  provider: string;
  model: string;
  timestamp: Date;
}
```

## تدفقات المستخدم الرئيسية

### تدفق 1: بدء محادثة جديدة
1. المستخدم يفتح التطبيق
2. يختار وكيل من قائمة الوكلاء النشطين
3. يتم الانتقال إلى شاشة المحادثة
4. يكتب الرسالة الأولى
5. يرسل الرسالة
6. يعرض الرد من الوكيل

### تدفق 2: تكوين API
1. المستخدم يذهب إلى الإعدادات
2. يختار المزود المطلوب
3. يدخل مفتاح API
4. يضغط على اختبار الاتصال
5. يحفظ الإعدادات

### تدفق 3: إدارة الأذونات
1. المستخدم يذهب إلى الإعدادات
2. يختار "الأذونات"
3. يفعل/يعطل الأذونات المطلوبة
4. يوافق على الأذونات عند الطلب
5. يحفظ التغييرات

## الخرائط الذهنية للتصميم

### خريطة تدفق المستخدم
```
المستخدم الجديد → تكوين API → إضافة وكيل → بدء محادثة → استخدام الأدوات
                    ↓
                إدارة الأذونات
                    ↓
                تخصيص الإعدادات
```

### خريطة الشاشات
```
الشاشة الرئيسية
├── شريط البحث
├── الوكلاء النشطين
├── المحادثات الأخيرة
└── إجراءات سريعة

شاشة المحادثة
├── رأس الشاشة
├── سجل المحادثة
├── حقل الإدخال
└── شريط الأدوات

شاشة الإعدادات
├── مفاتيح API
├── النماذج
├── الأذونات
└── المظهر

شاشة الأدوات
├── قائمة الأدوات
├── حالة الأدوات
└── التكوين

شاشة الذاكرة
├── سجل البيانات
├── السياق المحفوظ
└── إدارة الذاكرة
```
